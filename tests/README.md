# This folder contains E2E tests that Playwright will run
There are two folders for E2E tests:

- pages, which will test greater page functionality
- components, which will test components which rely on stores